// This file is for shared types at a glance.  We can colocate near the function as long as it's not reusued accross files, but for a few shared types, I like the patter of a single file to see most type info for a repo in one spot for small app.

export type ExampleType = string;
